#import "/templates/permanent.typ": *
#show: style

= Continuous Variable

A continuous variable describes numerical values that can hold an infinite number of values between any particular members. A continuous variable may be defined as $X = {1 <= x <= 2}$ whereas $X = {1, 1.25, 1.5, 1.75, 2}$ would be a *discrete variable*.

#hide[
  = Related
  #link("Discrete_Variable.typ")[Discrete_Variable]
]
