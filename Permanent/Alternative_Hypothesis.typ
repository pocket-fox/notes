#import "/templates/permanent.typ": *
#show: style

= Alternative Hypothesis

The assertion, the claim that must be proven to be true, otherwise it is
rejected. In court this would be the prosecution. It is rejected if it fails to
make its case or if its case is made with insufficient confidence, but can be
accepted if the case is strong. Represented by the symbol $H_A$.

//TODO: make connections
