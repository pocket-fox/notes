#import "/templates/permanent.typ": *
#show: style

= Sample Space

A sample space is a *set* consisting of all possible outcomes of a given
*experiment* and/or all possible *datapoints* in a given dataset. The sample
space is usually represented by the symbol $SS$.

#hide[
    = Related
    #link("Set.typ")[Set]
    #link("Experiment.typ")[Experiment]
    #link("Event.typ")[Event]
]
