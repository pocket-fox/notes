= Confidence Intervals

Confidence intervals describe the bounds in which, given a target confidence percent, a target
statistic lies within. Our goal is to be able to say: some statistic, $x$, with $95%$ confidence,
lies within some range, $x +- y$.

== t-Intervals

Given some distribution $overline(X)$ ($overline(X)$ must be at least approximately normal), a
$(1 - alpha) 100%$ confidence interval for 

== z-Intervals
